using System;
using static System.Math;

class Math 
{
    static void Main() 
    {
        double sqrt2 = Sqrt(2.0);
        Console.WriteLine($"sqrt(2) = {sqrt2}\n");
        Console.WriteLine($"sqrt2*sqrt2 = {sqrt2*sqrt2} (should be equal 2)\n");
    }
}
